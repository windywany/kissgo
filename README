1.install with apache (with rewrite module)
    1.1 edit your apache configure file httpd.conf or others,then add the following fragment in main or virtualhost

	<Directory "your_centims_webroot_dir">
		Options FollowSymLinks
		AllowOverride all
		Order allow,deny
		Allow from all
	</Directory>
    <IfModule mod_env.c>
        SetEnv SPECIAL_PATH /foo/bin
    </IfModule>

2.install with nginx

	server {
        listen       80;
        server_name  your_server_name;
        #access_log  off;
        #error_log   off;
        root your_centims_webroot_dir;
        location / {
            index index.php index.html index.htm;
            if (!-e $request_filename){
                rewrite “^(.*)$” index.php?__url=$1 last;
            }
        }
        location ~ /uploads/.+\.php$ {
        	return 403;
        }
        error_page  404              /404.html;
        location ~ \.php$ {
            fastcgi_pass   127.0.0.1:9000;
            fastcgi_index  index.php;
            fastcgi_param  SCRIPT_FILENAME  $document_root/$fastcgi_script_name;
            fastcgi_param  HTTP_REFERER  $http_referer;
            include        fastcgi_params;
        }
    }